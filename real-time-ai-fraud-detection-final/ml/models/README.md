# Model artifacts

Trained weights are intentionally excluded from Git. Place a complete Hugging Face export in `xlm_roberta_fraud_classifier/` for local inference, or distribute it as a private versioned model artifact.
